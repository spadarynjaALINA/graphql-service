import { Injectable } from '@nestjs/common';
// import { InjectModel } from '@nestjs/mongoose';
// import { Model } from 'mongoose';
// import { Track } from 'src/interfaces/interfaces';

@Injectable()
export class TracksService {
  // async create(createArtistsDto: Query): Promise<Track> {
  //   // const createdArtists = new this.artistsModel(createArtistsDto);
  //   return createdArtists.save();
  // }
  // async findTrackById(id): Promise<string> {
  //   return id;
  // }
}
